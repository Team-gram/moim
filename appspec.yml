version: 0.0
os: linux

files:
  - source: /
    destination: /opt/moim
permissions:
  - object: /opt/moim/
    owner: ubuntu
    group: ubuntu
    mode: 755
hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60
      runas: root
