package ajou.gram.moim.repository;

import ajou.gram.moim.domain.MoimPlaceUpperHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MoimPlaceUpperHistoryRepository extends JpaRepository<MoimPlaceUpperHistory, Long> {
}
