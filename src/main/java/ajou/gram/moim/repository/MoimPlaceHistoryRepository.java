package ajou.gram.moim.repository;

import ajou.gram.moim.domain.MoimPlaceHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MoimPlaceHistoryRepository extends JpaRepository<MoimPlaceHistory, Long> {
}
