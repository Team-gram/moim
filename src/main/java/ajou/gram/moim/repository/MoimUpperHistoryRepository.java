package ajou.gram.moim.repository;

import ajou.gram.moim.domain.MoimUpperHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MoimUpperHistoryRepository extends JpaRepository<MoimUpperHistory, Long> {
}
